import React, { Fragment } from "react";
import { ShoppingCart } from "./../../components";
const CartPage = () => {
  return (
    <Fragment>
      <ShoppingCart />
    </Fragment>
  );
};

export default CartPage;
